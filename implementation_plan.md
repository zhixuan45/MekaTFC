# MekaTFC 实施计划

## 调研结论

截至 2026-08-14，针对 Mekanism 与 TerraFirmaCraft（TFC）的精确检索未发现专门的公开兼容模组：

- Modrinth API 对 “Mekanism TerraFirmaCraft compatibility mod”、“Mekanism TFC addon”、“MekaTFC” 均无结果。
- GitHub 仓库检索对 “Mekanism TerraFirmaCraft”、“Mekanism TFC compatibility”、“MekaTFC” 均无结果。
- CurseForge 的搜索页面与两边主模组页面未显示明确的专门兼容项目；站点搜索存在反爬限制，因此该项作为辅助证据。

因此，本项目可以按“为 NeoForge 1.21.1 制作 Mekanism/TFC 专用兼容层”的方向推进。该结论不排除私人仓库、未发布源码、整合包内脚本或其他版本的零散实现。

## 当前工程状态

- NeoForge 21.1.240。
- Minecraft 1.21.1。
- Java toolchain 21；本机已发现 Zulu 21，但当前 `JAVA_HOME` 指向 Zulu 17。
- 当前仓库仍是 NeoForge 示例模板，尚未接入 Mekanism 与 TFC 的 API/运行时依赖。

## 默认实现范围

第一阶段只做低侵入、可配置的兼容基础：

1. 将 Mekanism 的基础原料链与 TFC 可获得资源建立配方互通，优先覆盖矿石、锭、粉、碎片及常用冶炼/处理产物。
2. 通过可选依赖与运行时检测，让缺少 Mekanism 或 TFC 时模组仍能安全加载；两者同时存在时才注册兼容内容。
3. 使用数据包/配方注册和标签映射承载可调整内容，Java 代码只负责跨模组 API 初始化、条件注册和必要的机器/能力适配。
4. 添加配置项，允许关闭特定配方组或调整兼容配方的启用状态，避免直接改变原版或 TFC 的核心平衡。
5. 添加最小化集成测试：缺少任一前置时可启动、两前置同时存在时注册成功、关键配方可解析且不会重复注册。

第一条默认红石来源已确定为两步配方：硫磺粉 1、铜粉 2、铁粉 2 无序合成为红石混合物，再将 1 个红石混合物放入 TFC 木桶并加入 1000 mB 水，密封后产出红石粉。标签上保留 TFC 已确认存在的 `c:powders/sulfur`，铜粉和铁粉使用社区通用的 `c:dusts/copper` 与 `c:dusts/iron`，以便接入不同模组提供的金属粉。该配方作为第一阶段基础材料入口；后续再增加 Mekanism 化学注入处理配方，作为更工业化的处理路线，不替换木桶配方。

## 第二阶段候选范围

以下内容不默认纳入第一版，需要单独确认设计：TFC 温度/锻造等级与 Mekanism 工厂的关系、TFC 金属与 Mekanism 化学处理链的完整映射、发电燃料平衡、矿物生成改写、容器/流体能力互通，以及 JEI/EMI 展示与本地化。

## 实施步骤

1. 确认目标版本、前置版本、加载器和第一版兼容边界。
2. 接入 Mekanism 与 TFC 的 compile/runtime 依赖，并修正 NeoForge 模组元数据中的可选依赖声明。
3. 建立统一的资源标签与配方映射表，避免把模组物品 ID 散落在业务代码中。
4. 实现条件化注册和配方生成；为每组映射保留独立命名空间与配置开关。
5. 删除模板示例内容，补充项目说明、本地化和配置默认值。
6. 使用 Java 21 运行 Gradle 的最小编译任务，再运行游戏启动/数据生成或集成测试验证注册结果。
7. 根据测试结果收紧版本范围，整理首个可安装构建与变更说明。

## 待审批的关键取舍

本计划默认第一版以“配方与资源标签兼容”为目标，不直接重写 TFC 的世界生成和 Mekanism 机器行为。红石材料先通过上述 TFC 木桶路线获得；化学注入路线安排在后续阶段。若要把温度、锻造等级或能源规则也纳入第一版，代码和测试范围会明显扩大，需要在动工前确定具体规则。
